// This describes what a typical .js file may look like
// as you build React Native apps.

// The below imports key tool's we'll be using from
// the 'react' and 'react-native' library that we've installed
import React from 'react';
import {StyleSheet, Text, App } from 'react-native';

// The below is called a 'class' which is like a blueprint
// Inside, we'll put most of our code.
default export MyComponent extends React.Component {
  render () {
    return (

    )
  }
}
